import React from "react";
import { BasePage } from "../layout/BasePage";

export const ProjectPage = (): React.JSX.Element => {
  return (
    <BasePage>
      <div className="text-black dark:text-green">ProjectPage</div>
    </BasePage>
  );
};
